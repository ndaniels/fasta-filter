[package]
name = "fasta-filter"
version = "0.2.0"
edition = "2021"
authors = ["Noah Daniels <noah_daniels@uri.edu>"]
description = "Filter a FASTA file and output a subset of the records on STDOUT"
license = "MIT"
readme = "README.md"

[dependencies]
bio = "3"
clap = { version = "4", features = ["derive"] }
